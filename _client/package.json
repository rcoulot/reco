{
  "name": "reco",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "watch-core": "cd typescript/reco/core && tsc --watch",
    "watch-ui": "cd typescript/reco/ui && tsc --watch",
    "watch-tests": "cd test && tsc --watch",
    "server": "http-server -a 0.0.0.0 -p 3000",
    "ssl-server": "http-server -p 443 -S -C ./ssl-cert.pem -K ./ssl-key.pem"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.7"
  }
}
